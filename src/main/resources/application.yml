spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/java_project
    username: java_project
    password: 123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
      